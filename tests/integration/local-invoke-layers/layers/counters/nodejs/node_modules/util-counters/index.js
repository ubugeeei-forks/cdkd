// Layer C — `util-counters`. No file overlap with the greetings layers,
// so this proves "multiple distinct layers stack at /opt".
module.exports = {
  count: (n) => `count=${n}`,
  source: 'counters',
};
